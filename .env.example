# Google Sheets API Credentials
# Copy this to .env.local and fill in your credentials
# For Railway/Vercel: Add these as environment variables in project settings

# RECOMMENDED: Use Service Account (never expires)
# Base64 encode your service-account.json: cat service-account.json | base64
GOOGLE_SERVICE_ACCOUNT=

# LEGACY: OAuth credentials (tokens expire regularly)
# Only use if not using Service Account
GOOGLE_CREDENTIALS=
GOOGLE_TOKEN=
